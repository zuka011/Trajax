stages:
  - verify
  - publish

variables:
  PIPELINE_ROOT: "visualizer"
  PYPI_PROJECT_NAME: "trajax-visualizer"
  PYPI_API_TOKEN: $PYPI_API_TOKEN_VISUALIZER 


include:
  - project: 'zurabm/ci-templates'
    ref: v1.3.0
    file:
      - '/python/verify.yml'
      - '/python/publish.yml'
